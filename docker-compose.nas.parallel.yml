# ============================================================
# AINCORE Blockchain — TerraMaster F2-423 NAS Deployment
# ============================================================
# Hardware: Intel Celeron N4505 (2C/2T), 4GB DDR4, 2x SATA Bay
# 
# Usage:
#   docker compose -f docker-compose.nas.yml up -d --build
#   docker compose -f docker-compose.nas.yml logs -f aincore-node
#   docker compose -f docker-compose.nas.yml down
# ============================================================

services:
  # ──────────────────────────────────────────────
  # Core Validator Node
  # ──────────────────────────────────────────────
  aincore-node:
    build:
      context: .
      dockerfile: Dockerfile
    image: aincore:dex-next
    container_name: aincore-dexnode-parallel
    restart: unless-stopped
    ports:
      - "9112:9102"   # P2P Gossipsub
      - "9012:9002"   # P2P Consensus
      - "8012:8002"   # REST API / RPC
    volumes:
      - ./nas_data_parallel:/root/.aincore/data
      - ./genesis.json:/root/.aincore/genesis.json:ro
    environment:
      - AINCORE_PORT=9002
      - AINCORE_API_PORT=8002
      - AINCORE_ENABLE_FAUCET=1
      - AINCORE_PERMISSIVE_CORS=1
      - AINCORE_GENESIS_PATH=/root/.aincore/genesis.json
      - AINCORE_PEERS=
      - RUST_LOG=info
      - RUST_BACKTRACE=1
    command: ["node", "--port", "9002", "--rpc-port", "8002", "--datadir", "/root/.aincore/data"]
    # NAS Resource Limits (F2-423: 2 cores, 4GB RAM)
    deploy:
      resources:
        limits:
          cpus: "1.5"        # Leave 0.5 core for TOS + system
          memory: 2560M      # 2.5GB — leave 1.5GB for TOS/indexer/OS
        reservations:
          cpus: "1.0"
          memory: 1024M
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    networks:
      - aincore-parallel-net

  # ──────────────────────────────────────────────
  # Indexer (Block Explorer Backend)
  # ──────────────────────────────────────────────
  aincore-indexer:
    build:
      context: .
      dockerfile: Dockerfile.indexer
    image: aincore-indexer:dex-next
    container_name: aincore-indexer-parallel
    restart: unless-stopped
    command: ["indexer"]
    volumes:
      - ./nas_data_parallel/indexer:/root/.aincore
    environment:
      - NODE_RPC_URL=http://aincore-node:8002/rpc
      - AINCORE_PERMISSIVE_CORS=1
      - AINCORE_INDEXER_BOOTSTRAP_BACKFILL=5000
      - AINCORE_INDEXER_BATCH_SIZE=2000
      - RUST_LOG=info
    ports:
      - "3012:3001"   # Indexer API
    depends_on:
      - aincore-node
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    networks:
      - aincore-parallel-net

  # ──────────────────────────────────────────────
  # Watchtower (Auto-update containers)
  # ──────────────────────────────────────────────
  # disabled for soak safety
  watchtower:
    profiles: ["manual-watchtower"]
    image: containrrr/watchtower
    container_name: aincore-dex-watchtower-disabled
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_POLL_INTERVAL=86400    # Check daily
      - WATCHTOWER_LABEL_ENABLE=false
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: 64M

volumes:
  aincore-data:
    driver: local

networks:
  aincore-parallel-net:
    driver: bridge
