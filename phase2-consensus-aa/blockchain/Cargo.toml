[package]
name = "blockchain"
version = "0.1.0"
edition = "2021"

[dependencies]
storage = { path = "../../common/storage" }
serde = { version = "1.0", features = ["derive"] }
# --- Tambahkan ini ---
sha2 = "0.10" # Atau versi terbaru yang kompatibel
# -------------------