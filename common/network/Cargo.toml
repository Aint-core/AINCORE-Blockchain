[package]
name = "network"
version = "0.1.0"
edition = "2021"

[dependencies]
rocksdb = "0.21.0"
storage = { path = "../storage" }
crypto = { path = "../crypto" }
rand = "0.8"
tokio = { version = "1.32", features = ["full"] }
once_cell = "1.18"